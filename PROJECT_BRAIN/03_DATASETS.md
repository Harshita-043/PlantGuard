# Dataset Status

No training or evaluation dataset directory is present in the application repository. The classification model metadata names PlantVillage, but the dataset itself, its exact source/version, splits, and evaluation procedure are **NOT VERIFIED** by this application audit.

The `ml/models/classification/class_names.json` file contains 38 labels. This confirms only the checked-in label list, not the dataset provenance, class balance, or model performance.

Do not download, preprocess, train, or modify ML assets as part of application stabilization.
