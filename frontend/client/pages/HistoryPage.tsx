export default function HistoryPage() {
  return (
    <section className="mx-auto max-w-2xl rounded-2xl border border-border bg-card p-8 text-center">
      <h2 className="text-xl font-semibold">Scan history is unavailable</h2>
      <p className="mt-2 text-sm text-muted-foreground">
        Scan results are not persisted by the current backend.
      </p>
    </section>
  );
}
